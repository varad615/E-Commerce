name: v_com
description: This is simple shopping app using firebase

publish_to: 'none'


version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter



  cupertino_icons: ^1.0.1
  firebase_core: "0.7.0"
  firebase_auth: "^0.20.0+1"
  cloud_firestore: "^0.16.0"

  #google fonts
  google_fonts: ^1.1.2
dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter


flutter:

  uses-material-design: true

  assets:
    - assets/images/

